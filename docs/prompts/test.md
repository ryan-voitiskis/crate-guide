## Testing Approach

Suggest tests that:
- Cover the critical paths and main use cases
- Include 1-2 important edge cases (not exhaustive)
- Are actually worth writing (good cost/benefit)

Keep test code simple and readable. Skip obvious getters/setters unless they have logic.
