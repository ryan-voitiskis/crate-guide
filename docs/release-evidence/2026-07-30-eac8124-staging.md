# Staging release evidence: `eac8124`

- Date: 2026-07-30
- Candidate branch: `codex/release-evidence-online`
- Candidate SHA: `eac8124ce4334d640ead8c7acc401b40177f68d3`
- Pull request: [#10](https://github.com/ryan-voitiskis/crate-guide/pull/10)
- Disposition: **staging evidence only; production unchanged**

## Decision

Keep the pull request in draft while the candidate remains on staging. The
implementation and staged runtime are healthy, but production promotion remains
gated on a production backup/restore plan and the separately authorised
interactive flows listed below.

No Discogs provider inquiry, portability decision, accountless mode, or offline
product mode is part of this candidate. Discogs acquisition remains enabled and
core to the application.

## Exact-head verification

- Hosted GitHub `Verify` run
  [30550063157](https://github.com/ryan-voitiskis/crate-guide/actions/runs/30550063157)
  passed at `eac8124`: both `application` and `database` jobs succeeded.
- A clean detached worktree at `eac8124`, using Node `24.18.1` and npm
  `11.16.0`, passed `npm ci`, `npm run verify`, `npm run build`,
  `npm run check:security-headers`, and
  `npm run check:client-bundle-budget`.
- The last full local `npm run verify:full`, including local pgTAP and live
  generated-schema type parity, passed at `cbc3e46`: 157 test files / 2,445
  application tests, 22 E2E passes plus one intentional skip, 81 browser tests,
  141 Edge tests, and 13 pgTAP files / 463 assertions. The only subsequent
  implementation change was the exact-head-tested CSP import-map hash fix.
- The staging build contains the staging Supabase reference and contains no
  production Supabase reference.
- Initial JavaScript is 1,097,413 raw bytes / 351,339 gzip bytes. The largest
  ordinary chunk is 127,476 raw / 36,752 gzip. Worker, WASM, CSS, and lazy
  boundaries remain within the checked-in budget.

## Dependency maintenance

- Every installed dependency is at the newest version allowed by the reviewed
  package ranges, and `npm audit` reports zero vulnerabilities.
- Clean installation explicitly permits only the reviewed lifecycle scripts.
  Optional `fsevents` scripts are denied by name, removing the pending-script
  warning without enabling an unreviewed native lifecycle.
- The remaining install warning is the transitive `glob@10.5.0` deprecation
  through `@vue/test-utils` and `js-beautify`; it is not a direct dependency and
  has no current npm advisory.
- Breaking upgrade tracks remain deliberate follow-up work: Pinia 4 and its
  Nuxt/testing adapters, Playwright 1.62 browser recertification,
  `prettier-plugin-tailwindcss` 0.8, TypeScript 7, and Zod 4.

## Staging deployment

- Cloudflare Pages project: `crate-guide-staging`
- Stable origin: <https://crate-guide-staging.pages.dev>
- Production branch for this isolated Pages project: `staging`
- Deployment:
  `ec1dedd8-7c87-4f74-814b-078bb78f3cfc`
  (<https://ec1dedd8.crate-guide-staging.pages.dev>)
- Cloudflare metadata reports environment `Production`, branch `staging`, and
  source `eac8124`.
- The build was produced and deployed from a clean detached worktree with every
  target selector and the full commit hash explicit.
- The stable origin serves the exact candidate. One-off Pages deployment URLs
  are not used for authenticated or OAuth testing because the configured
  `SITE_URL` and callback allow-list deliberately name the stable origin.

## Database and rollback evidence

- Supabase project: `crate-guide-staging`
  (`xrekloexiottvfueijgb`), explicitly separated from and denylisting production
  (`czlfiwivlgqhqezmywfx`).
- PostgreSQL `17.6.1.155` runs in `eu-central-1`.
- A pre-candidate public-schema dump was captured from the current-main staging
  baseline. SHA-256:
  `9a3c5bf6570301cce710527746c198a3dd8614d04a480f4aaf84b52e2262f109`.
- All 27 baseline migrations and these candidate migrations are applied and
  aligned with remote migration history:
  `20260722140000`, `20260722200000`, `20260722210000`,
  `20260722220000`, `20260723100000`, and `20260730140000`.
- A post-apply dry run reports no pending migrations.
- Current-main was exercised against the migrated database in a fresh session:
  protected hard reloads, all four library REST reads, crate creation/edit and
  membership changes, set save/reload, and settings load passed. Temporary data
  was removed.
- Constraint, privilege, RLS, RPC, receipt, and Evidence v2 guard checks passed.
  A temporary valid v2 write succeeded, a direct downgrade failed with
  PostgreSQL code `23514`, and all temporary receipt, track, record, and guard
  rows were removed.
- The old Pages app is not treated as a sufficient app-only rollback after an
  Evidence v2 write. Production promotion therefore requires an explicit
  database backup, restore target, restore verification, and owner before
  deployment. The staging Free project has no provider automatic backup.

## Hosted application evidence

### Library and account-scoped behavior

- Anonymous access to protected data is denied and protected routes redirect to
  `/login` with the intended return path.
- A synthetic confirmed staging account can sign in and retains the baseline
  record and four tracks.
- Crate create, edit, reload, membership add/remove, and pointer-driven delete
  confirmation passed.
- Set create, edit, reload, and delete passed.
- Preference persistence and restoration passed.
- Final baseline state is one profile, one record, four tracks, and zero crates
  and sets.

### Browsers and security headers

- Authenticated library, records, crates, and settings routes render correctly
  with zero console errors in Firefox and WebKit.
- Chromium interaction coverage passed in the in-app browser, including the
  nested delete-confirmation pointer path.
- A fresh local Chrome profile presents a non-bypassable browser safety warning
  for the staging origin. This is isolated to that browser profile; the in-app
  Chromium session, Firefox, WebKit, direct HTTP checks, and hosted API checks
  all reach the origin successfully.
- `/` and the SPA fallback route pass the repository's exact hosted header
  assertion: containment CSP, report-only CSP, staging-only Supabase HTTP/WSS
  origins, Discogs image origin, HSTS, frame denial, MIME sniffing denial,
  restrictive permissions, and no `x-powered-by`.
- Firefox exposed that CSP extraction omitted Nuxt's inline import map. The
  release head now hashes `type="importmap"` scripts, its regression test passes,
  and Firefox reports zero CSP or console errors on the deployed head.

### Evidence v2 and enrichment

- A hosted disposable 101-track library exercised client-equivalent enrichment
  chunks of 100 and 1 against deployment `cbc3e46`; the later `eac8124` change
  affects only browser CSP hashing.
- All 101 fill-empty updates returned `updated`, including the item at the chunk
  boundary, and reloaded with valid Evidence v2 plus BPM/key/mode.
- Exact replay returned the canonically identical stored response.
- A stale revision returned `stale_revision` without overwriting newer data.
- `read_library_snapshot` returned coherent records, tracks, crate membership,
  saved set, six selected preferences, and 101 v2 Evidence values.
- The disposable account and library were deleted. Direct database audit found
  no remaining profile, Auth row, library row, receipt, write guard, identity,
  or session.

### Edge Functions, scheduler, and Discogs boundary

- All six candidate Edge Functions are active on staging.
- The seven documented staging secrets are configured without recording their
  values in release evidence.
- Cleanup Functions reject missing/anonymous credentials. The authenticated
  account cleanup and service-only orphan cleanup contracts passed.
- `pg_cron`, `pg_net`, and Vault are enabled. The active
  `crate-guide-staging-account-cover-cleanup` job runs once per minute and stores
  only Vault references, not plaintext credentials, in its command.
- The account-cover scheduler's hosted 201-object rehearsal is recorded below
  with strict `pg_net` attribution:
  - hosted account deletion returned HTTP `200`, queued cleanup, and did not
    claim premature completion;
  - durable post-delete state contained 201 ordinary cleanup jobs and one
    account-cleanup outbox row;
  - settled scheduler-owned batches removed `100`, `100`, and `1` objects at
    approximately 57, 116, and 178 seconds;
  - settled responses contained two `processed`/incomplete results, one
    `processed`/complete result, and zero failures;
  - a separate control account/object and the global quota fingerprint remained
    unchanged until target completion; and
  - target and control artifacts were then removed, leaving zero cover objects,
    cleanup jobs, outbox rows, and user quota rows.
- Fixture administration used the staging legacy service-role key because the
  new secret-key client was not accepted by the current hosted Auth admin
  endpoint. This did not affect the candidate's hosted deletion Function or
  Vault-backed scheduler, both of which completed successfully.
- Authenticated Discogs request-token initiation returned a nonempty opaque
  token, and the temporary request state was cleared. No authorization page was
  opened, no collection import was attempted, and no provider communication or
  portability inquiry occurred.

## Remaining gates

1. Define and approve the production database backup, restore location,
   restoration verification, rollback owner, and go/no-go checklist.
2. When the user has capacity, authorise an interactive staging Discogs OAuth
   session and run folder listing, two-release import, retry/idempotency, and
   disconnect coverage. This is application use, not a provider inquiry.
3. Separately authorise transactional-email testing before sending confirmation
   or password-recovery messages. No email was sent during this rehearsal.
4. Review the deliberately deferred breaking dependency tracks as bounded,
   separately recertified upgrades.
5. Keep accountless/offline mode and the Discogs portability decision out of
   this release until product direction is reopened.

Until gates 1–3 are resolved, the safe release state is the current draft pull
request plus the isolated staging deployment. Production remains unchanged.
