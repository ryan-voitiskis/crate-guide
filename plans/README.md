# Implementation Plans

Generated by the improve skill on 2026-07-10. Execute in the order below
unless dependencies say otherwise. Each executor must read the selected plan
fully before starting, honor its STOP conditions, and update its row when done.

The plan is stamped against the completed track-enrichment branch at commit
`47c8b37`. Re-run its drift check immediately before implementation.

## Execution order and status

| Plan | Title                                                     | Priority | Effort | Depends on | Status |
| ---- | --------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Redesign the session track picker around physical records | P1       | M      | —          | DONE   |
| 002  | Attach the load-track dialog hook to real DOM             | P1       | S      | 001        | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 has no code or operational dependencies.
- Plan 002 closes a rendered-QA warning and dead test hook found while reviewing
  Plan 001's implementation.

## Findings considered and rejected

- Keep the flat track-result list and only enlarge its artwork: rejected
  because it continues to repeat one physical sleeve for every track and does
  not support release title, label, or catalogue-number lookup.
- Give every result card its own independently scrollable tracklist: rejected
  because nested scroll regions would be error-prone during a live session.
  Plan 001 uses a bounded preview and expands one record into the dialog's
  single outer scrolling region instead.
